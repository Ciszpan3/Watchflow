{
  "name": "watchflow-youtube-discovery",
  "version": "0.1.0",
  "private": true,
  "description": "Viewer-first YouTube discovery app for intentional, explainable and finite watch sessions.",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev:backend": "npm run dev --workspace backend",
    "dev:frontend": "npm run dev --workspace frontend",
    "build": "npm run db:generate && npm run build --workspace backend && npm run build --workspace frontend",
    "test": "npm run test --workspace backend && npm run test --workspace frontend",
    "lint": "npm run lint --workspace backend && npm run lint --workspace frontend",
    "db:generate": "npm run db:generate --workspace backend",
    "db:migrate": "npm run db:migrate --workspace backend",
    "db:status": "npm run db:status --workspace backend",
    "db:validate": "npm run db:validate --workspace backend"
  },
  "engines": {
    "node": ">=20.19"
  },
  "allowScripts": {
    "prisma@7.10.0": true,
    "@prisma/engines@7.10.0": true,
    "esbuild@0.28.2": true,
    "esbuild@0.25.12": true
  }
}
